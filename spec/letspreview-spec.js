'use strict';

const letspreview = require('../letspreview');

describe('describe title for test grouping', () => {
    // jasmine.DEFAULT_TIMEOUT_INTERVAL = 15000;
    it('describe what should happen under a specific condition.', (done) => {

    });
});
